import { ArrowRightLeft, Check, ExternalLink, ImageOff, Star } from 'lucide-react'

/** Evidence card for a listing in the current product category. */
export default function ProductCard({ product, selected, onToggleComparison }) {
  const specs = Object.entries(product.specs || {}).slice(0, 3)
  return <article className="group relative overflow-hidden rounded-2xl border border-slate-200 bg-white p-4 shadow-sm transition duration-200 hover:-translate-y-1 hover:shadow-lg"><div className="flex gap-3"><div className="grid h-24 w-24 shrink-0 place-items-center overflow-hidden rounded-xl bg-slate-100">{product.imageUrl ? <img src={product.imageUrl} alt="" className="h-full w-full object-cover" onError={event => { event.currentTarget.style.display = 'none' }} /> : <ImageOff className="text-slate-400" />}</div><div className="min-w-0 flex-1"><h3 className="line-clamp-2 text-sm font-bold text-slate-800">{product.name}</h3><p className="mt-2 text-xl font-extrabold text-indigo-700">{product.price}</p><span className="mt-1 inline-block max-w-full truncate rounded-full bg-slate-100 px-2 py-1 text-[10px] font-semibold text-slate-600">{product.source}</span>{product.rating != null && <p className="mt-2 flex items-center gap-1 text-xs font-semibold text-amber-600"><Star size={14} fill="currentColor" />{product.rating}{product.rating_count != null && <span className="text-slate-500">({Number(product.rating_count).toLocaleString('en-IN')})</span>}</p>}{product.delivery && <span className="mt-2 inline-block rounded-full bg-emerald-50 px-2 py-1 text-[10px] font-semibold text-emerald-700">{product.delivery}</span>}</div></div><div className="mt-4 space-y-1 text-xs text-slate-500">{specs.map(([key, value]) => <p className="flex justify-between gap-3" key={key}><span>{key}</span><strong className="truncate text-slate-700">{value}</strong></p>)}</div><div className="mt-4 flex items-center justify-between"><button type="button" onClick={() => onToggleComparison(product)} className={`rounded-xl px-3 py-2 text-xs font-bold transition ${selected ? 'bg-indigo-600 text-white hover:bg-indigo-700' : 'bg-indigo-50 text-indigo-700 hover:bg-indigo-100'}`}>{selected ? <><Check className="mr-1 inline" size={14} />Selected</> : <><ArrowRightLeft className="mr-1 inline" size={14} />Compare</>}</button>{product.link && <a href={product.link} target="_blank" rel="noreferrer" className="rounded-lg p-2 text-slate-400 hover:bg-slate-100 hover:text-indigo-600"><ExternalLink size={16} /></a>}</div></article>
}
