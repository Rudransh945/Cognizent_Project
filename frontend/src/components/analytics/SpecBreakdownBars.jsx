export default function SpecBreakdownBars({ fields, products }) {
  if (!fields.length) return <section className="chart-card"><h2>Product-specific feature breakdown</h2><p className="chart-caption">No source-confirmed feature details are available for these listings yet.</p></section>
  return <section className="chart-card"><h2>Product-specific feature breakdown</h2><p className="chart-caption">Fields are selected from source-confirmed evidence. “Not listed” means the source did not confirm that detail.</p><div className="mt-5 space-y-4">{fields.map(field => <div key={field.id}><p className="mb-2 text-sm font-bold text-slate-800">{field.label}</p><div className="grid gap-2">{products.map((product, index) => <div className="flex items-center justify-between gap-3 rounded-xl bg-slate-50 px-3 py-2 text-xs" key={`${product.name}-${index}`}><span className="max-w-[46%] truncate font-medium text-slate-600">{product.name}</span><strong className="max-w-[52%] truncate text-right text-slate-800">{field.values[index] || 'Not listed'}</strong></div>)}</div></div>)}</div></section>
}
